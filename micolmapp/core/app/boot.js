/**
 * Arranque de aplicación (placeholder).
 * Hoy el bootstrap real ocurre en `miColmApp.html` (auth + pantallas).
 */
export function registerAppShell() {
  // Reservado para futura extracción (init loading, error boundary, etc.)
}
