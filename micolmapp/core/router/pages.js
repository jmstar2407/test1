/**
 * IDs de páginas principales (DOM `#page-{id}`, nav `#navbtn-{id}`).
 * La lógica de navegación sigue en `miColmApp.html` (`showPage`) hasta migrar a `core/router`.
 */
export const APP_PAGE_IDS = Object.freeze([
  'pos',
  'caja',
  'facturas',
  'inventario',
  'estadisticas',
  'config',
]);
